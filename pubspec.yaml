name: One
description: A Flutter project for login, group creation, and profile pages.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1   # ✅ added

flutter_icons:
  android: true
  ios: false
  image_path: "image/logo.png"     # ✅ your logo file path

flutter:
  uses-material-design: true
