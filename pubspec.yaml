name: one
description: A Flutter project for login, group creation, and profile pages.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0
  
  # For secure token/credential storage
  flutter_secure_storage: ^9.0.0

  # 👇 Dependencies for Offline Data Store (SQFLite)
  sqflite: ^2.3.0 
  path: ^1.8.3 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/logo.png"

flutter:
  uses-material-design: true
